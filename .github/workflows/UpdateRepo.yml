#Do NOT Edit.
name: Update-Packages
on: [push]
jobs:
  Update-Packages-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event_name }}."
      - run: echo "${{ runner.os }}."
      - run: echo "${{ github.ref }} ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "${{ github.repository }}."
      - run: echo "Runner Ready."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: chmod +x ./updaterepo.sh
      - run: ./updaterepo.sh
#You can edit email,username & message.
      - run: git config --global user.email "turannul000@gmail.com"
      - run: git config --global user.name "Repo BOT"
      - run: git add "${{ github.workspace }}"
      - run: git commit -a -m "Packages updated. (Auto Action)"
      - run: git push
      - run: echo "${{ job.status }}."
